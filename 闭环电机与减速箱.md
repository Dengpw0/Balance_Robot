```c
TargetPosition=Circle*1560*1.04; //目标位置=目标圈数*1560
	                                   //10ms读取一次编码器(即100HZ)，电机减速比为20，霍尔编码器精度13，AB双相组合得到4倍频，
	                                   //则转1圈编码器读数为30*13*4=1560，电机转速=Encoder*100/1040r/s 使用定时器2
	                                   //1.04是误差系数，电机本身存在误差，可根据实际情况调整该系数以提高控制精度
```

角度环在pid代码里加入了

目标位置为目标圈数*1560

```c
CurrentPosition+=Encoder; //编码器读数(速度)积分得到位置
//编码器读数累加，也就是当前电机转的圈数
```

```c
PWM=Position_FeedbackControl(TargetCircle, CurrentPosition); //位置闭环控制
//输入当前转的圈数，和减速器目标圈数，目标圈数*13*30*13*误差系数
```

pid计算返回pwm输出值
